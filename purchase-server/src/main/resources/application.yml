server:
  port: 8666

spring:
  cache:
    type: redis
  profiles:
    include:
      - core
  kafka:
    bootstrap-servers: localhost:9092
  redis:
    cache:
      host: localhost
      port: 6379

#logging:
#  level:
#    org:
#      hibernate:
#        type:
#          descriptor:
#            sql:
#              BasicBinder: TRACE
#        SQL: DEBUG